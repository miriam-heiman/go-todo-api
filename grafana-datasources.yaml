apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true

  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    editable: true
    jsonData:
      tracestoLogs:
        datasourceUid: Loki
        tags: ['job', 'instance']
        mappedTags: [{key: 'service.name', value: 'service'}]